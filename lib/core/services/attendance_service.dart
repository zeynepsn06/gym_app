class AttendanceService {
  static int attendedDays = 12;

  static void addAttendance(String memberId) {
    // ŞİMDİLİK MOCK
    attendedDays++;

    // İleride burada:
    // API POST /attendance
    // Firebase write
  }
}
